import React from "react";
import { VisionContainer, VisionContent, VisionH1 } from "./VisionElements";

const VisionSection = () => {
  return (
    <VisionContainer id="vision">
      <VisionContent>
        <VisionH1>Vision</VisionH1>
      </VisionContent>
    </VisionContainer>
  );
};

export default VisionSection;
